# Getting Started with Pokédex App

This project was created as part of the recruitment task as frontend in **Edtech Telkom Digital** using [React.js](https://github.com/facebook/create-react-app).

## Available Scripts

To run this app, you must change your directory to the project directory, and then you can run:

### `npm install`

and then:

### `npm start`

This command would run the app in development mode.\    
Open [http://localhost:3000](http://localhost:3000) to view the project in the browser.
